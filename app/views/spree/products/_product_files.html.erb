<ul class="product_files">
  <% @product.product_files.each do |product_file| %>
    <li>
      <%= link_to product_file.display_name, main_app.rails_blob_path(product_file.attachment, disposition: "attachment") %>
    </li>
  <% end %>
</ul>
